import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { text, select, boolean } from '@storybook/addon-knobs';
import { LinkButton, Typography } from 'src/shared-components';
import { action } from '@storybook/addon-actions';
import { COLORS } from 'src/constants';
import { NeckGlyph } from 'src/svgs/glyphs';

<Meta title="Buttons/LinkButton" component={LinkButton} />

# LinkButton

## Usage

```tsx
import { LinkButton } from 'radiance-ui';
```

<ArgsTable of={LinkButton} />

<Typography.Heading>Default</Typography.Heading>

<Canvas>
  <Story name="Default">
    <LinkButton.Container>
      <LinkButton href="https://www.curology.com">Primary</LinkButton>
      <LinkButton buttonType="secondary">Secondary</LinkButton>
      <LinkButton buttonType="tertiary">Tertiary</LinkButton>
      <LinkButton buttonType="quaternary">Quaternary</LinkButton>
      <LinkButton disabled>Disabled</LinkButton>
    </LinkButton.Container>
  </Story>
</Canvas>

export const MockLink = ({ to, children, ...rest }) => (
  <a href={to} {...rest}>
    {children}
  </a>
);

<Typography.Heading>React Router Link</Typography.Heading>

<Canvas>
  <Story name="React Router Link">
    <LinkButton.Container>
      <LinkButton
        to="https://www.google.com/search?q=react+router+link"
        as={MockLink}
      >
        Router Link
      </LinkButton>
    </LinkButton.Container>
  </Story>
</Canvas>

<Typography.Heading>With Color</Typography.Heading>

<Canvas>
  <Story name="With Color">
    <LinkButton.Container>
      <LinkButton
        buttonColor={COLORS.statusRed}
        href="https://www.curology.com"
      >
        Primary
      </LinkButton>
      <LinkButton buttonColor={COLORS.statusRed} buttonType="secondary">
        Secondary
      </LinkButton>
      <LinkButton buttonColor={COLORS.statusRed} buttonType="tertiary">
        Tertiary
      </LinkButton>
      <LinkButton buttonColor={COLORS.statusRed} buttonType="quaternary">
        Quaternary
      </LinkButton>
      <LinkButton buttonColor={COLORS.statusRed} disabled>
        Disabled
      </LinkButton>
    </LinkButton.Container>
  </Story>
</Canvas>

<Typography.Heading>With Controls</Typography.Heading>

<Canvas>
  <Story name="With Controls">
    <LinkButton.Container>
      <LinkButton
        buttonType={select(
          'buttonType',
          ['primary', 'secondary', 'tertiary', 'quaternary'],
          'primary',
        )}
        buttonColor={select('buttonColor', COLORS, COLORS.primary)}
        disabled={boolean('disabled', false)}
        onClick={action('You clicked a button')}
        textColor={text('textColor', '')}
      >
        {text('children', 'Click it!')}
      </LinkButton>
    </LinkButton.Container>
  </Story>
</Canvas>

#### LinkButton Notes

`LinkButton` will render a 'button-like' link for directing/linking to the path
specified. This component can work with React Router's `Link`/`NavLink` by passing
in the router component as a prop ---> `<LinkButton to='/path' as={Link}> ....`.

`<LinkButton.Container>` can be used to provide spacing between multiple
buttons and behavior on various screen sizes.

We should generally try to use the default button color when possible.
Only for special cases should we need to use a different button color.

