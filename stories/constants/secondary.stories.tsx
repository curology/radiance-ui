import { Meta } from '@storybook/react';

export * from './index.stories';

export default {
  title: 'CONSTANTS/Constants/Secondary',
  parameters: {
    theme: 'secondary',
  },
  excludeStories: /.*/,
} as Meta;
