import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { text, select, number, boolean } from '@storybook/addon-knobs';
import { Carousel, Typography } from 'src/shared-components';
import { COLORS, SPACER } from 'src/constants';
import { NeckGlyph } from 'src/svgs/glyphs';
import styled from '@emotion/styled';

<Meta title="Carousel" component={Carousel} />

# Carousel

Carousels should be used to provide valuable information or additional context on a page. One of the best examples of a Carousel is for product recommendations.

## Usage

```jsx
import { Carousel } from 'radiance-ui';
```

<ArgsTable of={Carousel} />

export const Card = styled(Carousel.Card)`
  background-color: ${COLORS.border};
  text-align: center;
  padding: ${SPACER.large};
`;

export const cards = [
  <Card key={1}>Card 1</Card>,
  <Card key={2}>Card 2</Card>,
  <Card key={3}>Card 3</Card>,
  <Card key={4}>Card 4</Card>,
  <Card key={5}>Card 5</Card>,
];

<Typography.Heading>Default - 1 card</Typography.Heading>

<Canvas>
  <Story name="1 card">
    <Carousel numCardsVisible={1}>{cards}</Carousel>
  </Story>
</Canvas>

<Typography.Heading>2 cards - Infinite</Typography.Heading>

<Canvas>
  <Story name="2 cards + infinite">
    <Carousel numCardsVisible={2} infinite>
      {cards}
    </Carousel>
  </Story>
</Canvas>

<Typography.Heading>Default - 3 cards</Typography.Heading>

<Canvas>
  <Story name="3 cards">
    <Carousel numCardsVisible={3}>{cards}</Carousel>
  </Story>
</Canvas>

<Typography.Heading>Autoplay (5 seconds)</Typography.Heading>

<Canvas>
  <Story name="autoplay">
    <Carousel numCardsVisible={1} autoplay>
      {cards}
    </Carousel>
  </Story>
</Canvas>

<Typography.Heading>Autplay Infinite</Typography.Heading>

<Canvas>
  <Story name="autoplay infinite">
    <Carousel numCardsVisible={1} autoplay infinite>
      {cards}
    </Carousel>
  </Story>
</Canvas>

<Typography.Heading>Hide Dots</Typography.Heading>

<Canvas>
  <Story name="Hide Dots">
    <Carousel numCardsVisible={1} hideDots>
      {cards}
    </Carousel>
  </Story>
</Canvas>

<Typography.Heading>Hide Arrows (swipe to navigate)</Typography.Heading>

<Canvas>
  <Story name="Hide Arrows (Swipe Navigation)">
    <Carousel numCardsVisible={1} hideArrows>
      {cards}
    </Carousel>
  </Story>
</Canvas>

<Typography.Heading>Bottom Right Aligned Arrows</Typography.Heading>

<Canvas>
  <Story name="Bottom Right Aligned Arrows">
    <Carousel numCardsVisible={2} bottomRightAlignedArrows>
      {cards}
    </Carousel>
  </Story>
</Canvas>

export const CarouselContainer = styled.div`
  display: flex;
  height: 150px;
  width: 375px;
  align-items: center;
  background-color: ${({ bgColor }) => bgColor || COLORS.background};
`;

<Typography.Heading>Secondary Style - dots are white</Typography.Heading>

<Canvas>
  <Story name="Secondary Style (All dots are white)">
    <CarouselContainer bgColor={COLORS.secondary}>
      <Carousel numCardsVisible={1} carouselType="secondary">
        {cards}
      </Carousel>
    </CarouselContainer>
  </Story>
</Canvas>

<Typography.Heading>With Controls</Typography.Heading>

<Canvas>
  <Story name="With Controls">
    <CarouselContainer bgColor={text('Background color', COLORS.background)}>
      <Carousel
        autoplay={boolean('autoplay', true)}
        autoplaySpeed={number('autoplaySpeed, in milliseconds', 5000)}
        carouselType={select(
          'carouselType',
          ['primary', 'secondary'],
          'primary',
        )}
        centerMode={boolean('centerMode', true)}
        hideArrows={boolean('hideArrows', false)}
        bottomRightAlignedArrows={boolean('bottomRightAlignedArrows', false)}
        hideDots={boolean('hideDots', false)}
        infinite={boolean('infinite', false)}
        numCardsVisible={number('numCardsVisible', 1)}
      >
        {cards}
      </Carousel>
    </CarouselContainer>
  </Story>
</Canvas>

### Notes

The property `numCardsVisible` will define the width of the Carousel Container.

An array of `Carousel.Card` must be used for the carousel content. It includes the base styles for the Card which may be extended as shown above.
