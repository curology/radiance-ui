const transformTemplateForUtilLocation = require('./transformTemplateForUtilLocation.js');
const { UTIL_LOCATION } = require('./constants');

module.exports = transformTemplateForUtilLocation(UTIL_LOCATION);
