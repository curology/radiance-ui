import { MildIrritationEmoji, MildIrritationEmojiSecondary } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(MildIrritationEmoji, MildIrritationEmojiSecondary, props);
