import { AnxiousEmoji, AnxiousEmojiSecondary } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(AnxiousEmoji, AnxiousEmojiSecondary, props);
