import { NoticeableAcneEmoji } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(NoticeableAcneEmoji, NoticeableAcneEmoji, props);
