import { ConfusedSelectedEmoji, ConfusedSelectedEmojiSecondary } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(ConfusedSelectedEmoji, ConfusedSelectedEmojiSecondary, props);
