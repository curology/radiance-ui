import { OverwhelmedEmoji, OverwhelmedEmojiSecondary } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(OverwhelmedEmoji, OverwhelmedEmojiSecondary, props);
