import { HeartSelectedEmoji } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(HeartSelectedEmoji, HeartSelectedEmoji, props);
