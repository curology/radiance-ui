import { SatisfiedEmoji, SatisfiedEmojiSecondary } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(SatisfiedEmoji, SatisfiedEmojiSecondary, props);
