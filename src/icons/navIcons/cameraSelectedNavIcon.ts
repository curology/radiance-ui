import { CameraSelectedNavIcon } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(CameraSelectedNavIcon, CameraSelectedNavIcon, props);
