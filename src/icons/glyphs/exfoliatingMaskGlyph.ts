import ExfoliatingMaskGlyphPrimary from './svgs/exfoliating-mask-glyph.svg';
import { useIcon, IconProps } from '../../shared-components/icon';

export const ExfoliatingMaskGlyph = (props: IconProps) =>
  useIcon(ExfoliatingMaskGlyphPrimary, ExfoliatingMaskGlyphPrimary, props);
