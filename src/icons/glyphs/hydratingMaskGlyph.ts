import { HydratingMaskGlyph } from './svgs';
import { IconProps, useIcon } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(HydratingMaskGlyph, HydratingMaskGlyph, props);
