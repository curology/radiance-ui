import SuperbottleGlyphPrimary from './svgs/superbottle-glyph.svg';
import { useIcon, IconProps } from '../../shared-components/icon';

export const SuperbottleGlyph = (props: IconProps) =>
  useIcon(SuperbottleGlyphPrimary, SuperbottleGlyphPrimary, props);
