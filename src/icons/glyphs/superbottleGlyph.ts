import { SuperbottleGlyph, SuperbottleGlyphSecondary } from './svgs';
import { IconProps, useIcon } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(SuperbottleGlyph, SuperbottleGlyphSecondary, props);
