import { VitaminCGlyph, VitaminCGlyphSecondary } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(VitaminCGlyph, VitaminCGlyphSecondary, props);
