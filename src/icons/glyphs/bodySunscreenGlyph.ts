import { BodySunscreenGlyph } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(BodySunscreenGlyph, BodySunscreenGlyph, props);
