import BodySunscreenGlyphPrimary from './svgs/body-sunscreen-glyph.svg';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(BodySunscreenGlyphPrimary, BodySunscreenGlyphPrimary, props);
