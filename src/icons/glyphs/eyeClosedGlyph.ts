import { EyeClosedGlyph } from './svgs';
import { type IconProps, useIcon } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(EyeClosedGlyph, EyeClosedGlyph, props);
