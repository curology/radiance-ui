import VeryOilyGlyphPrimary from './svgs/very-oily-glyph.svg';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(VeryOilyGlyphPrimary, VeryOilyGlyphPrimary, props);
