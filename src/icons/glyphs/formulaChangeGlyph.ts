import { FormulaChangeGlyph, FormulaChangeGlyphSecondary } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(FormulaChangeGlyph, FormulaChangeGlyphSecondary, props);
