import TimeNeverGlyphPrimary from './svgs/time-never-glyph.svg';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(TimeNeverGlyphPrimary, TimeNeverGlyphPrimary, props);
