import { CalendarMostOfTheTimeGlyph } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(CalendarMostOfTheTimeGlyph, CalendarMostOfTheTimeGlyph, props);
