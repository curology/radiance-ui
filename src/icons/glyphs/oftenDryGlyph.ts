import OftenDryGlyphPrimary from './svgs/often-dry-glyph.svg';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(OftenDryGlyphPrimary, OftenDryGlyphPrimary, props);
