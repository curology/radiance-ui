import { MostOfTheTimeGlyph, MostOfTheTimeGlyphSecondary } from './svgs';
import { useIcon, IconProps } from '../../shared-components/icon';

export default (props: IconProps) =>
  useIcon(MostOfTheTimeGlyph, MostOfTheTimeGlyphSecondary, props);
