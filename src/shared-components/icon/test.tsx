describe('Icon', () => {
  it('displays the primary icon with the primary theme', () => {
    expect(1).toEqual(1);
  });

  it('displays the secondary icon with the secondary theme', () => {
    expect(1).toEqual(1);
  });
});
